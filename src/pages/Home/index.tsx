import { FC, ReactElement } from "react";

const Home: FC = (): ReactElement => {
  return <div style={{
    paddingTop: '50px',
    textAlign: 'center'
  }}>Home page</div>;
};

export default Home;
